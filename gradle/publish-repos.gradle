apply plugin: "maven-publish"

publishing {
    repositories {
        maven {
            url "https://nexus.gcp.inf-impact.net/repository/thirdparty/"
            credentials {
                username findProperty("nexusUsername")
                password findProperty("nexusPassword")
            }
        }
    }
}
